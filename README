gulp for compress

git  clone https://github.com/lingyucet/Node-share.git   // 下载

npm install  //安装

gulp testWatch //执行 监控


提示：git的使用方法

mkdir  Node-share  //创建版本库
cd     Node-share
touch  README      //创建readme文件
git    add  README //添加到上传序列
git    commit -m "upload README file"  //缓存提交
git    remote  add origin  https://github.com/lingyucet/Node-share.git //配置上传目标库
git    push  -u  origin  master  //上传文件